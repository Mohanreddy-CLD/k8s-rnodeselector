apiVersion: v1
kind: Pod
metadata:
  name: node-selector-pod
  labels:
    purpose: demonstrate-node-selector 
    env: test
spec:
  containers:
  - name: nginx
    image: nginx
    imagePullPolicy: Always
  nodeSelector:
    disktype: ssd